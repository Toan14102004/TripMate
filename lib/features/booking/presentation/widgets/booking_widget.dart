// TODO: Booking 기능에 필요한 위젯을 구현하세요.
import 'package:flutter/material.dart';

class BookingWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Booking Widget'),
    );
  }
}